
<table class="table table-bordered table-striped table-condensed flip-content table-list">
    <thead>
    <tr class="uppercase">
        <th width="20">{{ "Sl No."|trans }}</th>
        <th width="100">{{ 'Application Type'|trans }}</th>
        <th width="100">{{ 'Application ID'|trans }}</th>
        <th width="100">{{ 'Application Date'|trans }}</th>
        <th width="100">{{ 'Request From'|trans }}</th>
       <th width="200">{{  'Applicant Name'|trans }}</th>
         <th width="200">{{ 'Upozila'|trans }}</th>
         <th width="200">{{ 'Mouza'|trans }}</th>
        <th width="200">{{ 'J. L. No'|trans }}</th>
        <th width="200">{{ 'Khatian No'|trans }}</th>
        <th width="200">{{ 'Delivery Type'|trans }}</th>
        <th width="200">{{ 'Probably Delivery Date'|trans }}</th>
       <th width="200">{{ 'Delivery Date'|trans }}</th>
       <th width="200">{{ 'Court Fee'|trans }}</th>
        {#
      <th width="200">{{ 'Delivery Date Duration'|trans }}</th>&#35;&#125;#}
    </tr>
    </thead>
    <tbody>
    {% if courtFeeRegisters is not empty %}
        {% for index , courtFeeRegister in courtFeeRegisters %}
            <tr>
                <td>{{ index + 1 }}</td>
                <td>{{ courtFeeRegister.serviceRequest.type|trans }}</td>
                <td>{{ courtFeeRegister.serviceRequest.id }}</td>
                <td>{{ courtFeeRegister.serviceRequest.createdAt|date('Y-m-d') }}</td>
                <td>{{ courtFeeRegister.serviceRequest.requestFrom|trans }}</td>
                <td>{{ courtFeeRegister.serviceRequest.name|default('N/A') }}</td>
                <td>{{ courtFeeRegister.upozila.name|default('N/A') }}</td>
                <td>{{ courtFeeRegister.mouza.name|default('N/A') }}</td>
                <td>{{ courtFeeRegister.jlNo|default('N/A') }}</td>
                <td>{{ courtFeeRegister.khatianNo|default('') }}</td>
                <td>{{ courtFeeRegister.serviceRequest.DeliveryMethod|trans }}</td>
                <td>{{ courtFeeRegister.serviceRequest.estimateDeliveryAt|date('Y-m-d') }}</td>
                <td>{{ courtFeeRegister.serviceRequest.deliveredAt|date('Y-m-d') }}</td>
                <td>{{ courtFeeRegister.serviceRequest.CourtFee|default }}</td>
                {#<td>{{ 'now'|date('Y-m-d') - (deliveryKhatianRegister.serviceRequest.deliveredAt|date('Y-m-d')) }}</td>#}

            </tr>
        {% endfor %}
    {% else %}
        <tr class="ta-center">
            <td colspan="16">{{ 'No record found'|trans }}</td>
        </tr>
    {% endif %}
    </tbody>
</table>

<div class='dataTables_paginate paging_bootstrap pagination'>
    {{ knp_pagination_render(courtFeeRegisters) }}
</div>
