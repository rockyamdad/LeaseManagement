{% extends '::base.html.twig' %}

{% block title %}{{ parent() }} : {{ 'User password change'|trans }} {% endblock %}

{% block body %}
    <div class="col-md-12">
        <div class="portlet box blue">
            <div class="portlet-title">
                <div class="caption">
                    {{ 'User password change'|trans }}
                </div>
            </div>
            <div class="portlet-body">
                {{ include('_elements/_back_to_list_button.html.twig', {url: path('users_home'), label: 'Back to List'|trans, 'icon': 'chevron-left'}) }}
                {% include "RbsUserBundle:User:update.password_content.html.twig" %}
            </div>
        </div>
    </div>
{% endblock %}